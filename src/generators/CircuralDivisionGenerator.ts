import { Node } from "../strategies/Node";
import { AbstractGenerator } from "./AbstractGenerator";

export class CircuralDivisionGenerator extends AbstractGenerator {
  constructor() {
    super("Circural Division");
  }

  generate(grid: Node[][]): Node[] {
    return [];
  }
}
